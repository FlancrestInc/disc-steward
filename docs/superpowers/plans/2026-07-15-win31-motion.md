# Win31 Motion Adoption Implementation Plan

> **For agentic workers:** REQUIRED: Use superpowers:subagent-driven-development (if subagents available) or superpowers:executing-plans to implement this plan. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Add reduced-motion-safe Win31 window and disclosure feedback to Disc Steward without delaying its native page navigation.

**Architecture:** Extend the shared motion package with a disclosure primitive, rebuild it, and vendor the compiled CSS in Disc Steward. The application serves and links that artifact exactly as it does core CSS, then applies only stable motion classes to the app window and existing disclosure elements.

**Tech Stack:** CSS transform/opacity animations, `prefers-reduced-motion`, Python standard-library HTTP server, Pytest, pytest-playwright.

---

### Task 1: Shared motion contract

**Files:**
- Modify: `/home/ryan/projects/style/packages/motion/src/motion.css`
- Rebuild: `/home/ryan/projects/style/packages/motion/dist/motion.css`

- [x] Add `ds-motion-disclosure` to animate only newly revealed detail content with transform/opacity.
- [x] Ensure the existing reduced-motion policy disables the new primitive.
- [x] Run Style package checks and build output validation.

### Task 2: Disc Steward asset seam and markup

**Files:**
- Modify: `disc_steward/web.py`, `tests/test_web_metadata_playback.py`
- Create: `disc_steward/static/win31-motion.css`

- [x] Write failing tests for the versioned motion stylesheet link, its allowlisted response, and window/disclosure motion hooks.
- [x] Serve and link the built motion artifact after core CSS; apply `ds-motion-enter-window` to the window and `ds-motion-disclosure` to existing disclosure elements.
- [x] Run focused web-render tests.

### Task 3: Browser accessibility verification

**Files:**
- Modify: `tests/browser/test_review_ui.py`

- [x] Verify the standard browser path has the motion hooks and a reduced-motion emulation removes animation from the window.
- [x] Run browser coverage, package compilation, and diff checks.
